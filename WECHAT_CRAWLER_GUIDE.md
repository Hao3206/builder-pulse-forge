# 公众号资讯采集

本项目采用“服务器扫码登录 -> 抓取已发表记录 -> 后台候选池 -> 管理员确认发布”的流程，不依赖草稿箱 API 直接导入官网。

## 服务器要求

服务器需要安装 Chromium，并配置：

```env
CHROMIUM_EXECUTABLE_PATH=/usr/bin/chromium
WECHAT_CRAWLER_PROFILE_DIR=/var/lib/zdeaee/wechat-crawler-profile
```

Ubuntu/Debian 可按服务器发行版安装 `chromium`。运行网站的系统用户必须拥有登录态目录的读写权限。

公众号后台登录态权限较高，目录不得放入网站静态目录、Git 仓库或普通备份包。建议限制为服务进程用户可读写。

## 后台操作

1. 进入 `/admin/wechat-sync`。
2. 点击“打开扫码窗口”。服务器会启动无界面 Chromium 并返回公众号后台截图。
3. 使用公众号管理员微信扫描截图中的二维码。
4. 页面显示“已登录”后，点击“采集最近 20 篇”。
5. 新文章进入“待确认”列表，不会直接公开。
6. 预览原文并勾选需要发布的文章，点击“确认发布”。
7. 不需要的文章可设为“已忽略”，也可以恢复待确认。

系统按公众号原文 URL 和正式新闻标题去重。点击“退出公众号”会关闭当前浏览器会话并删除服务器保存的登录态。

## 部署注意

- Chromium 需要 `--no-sandbox` 和足够的共享内存；本项目已加入对应启动参数。
- 单机部署使用本地持久化目录即可。
- 多实例部署时，采集接口需要固定到同一实例，登录态目录也需要独立规划。
- 登录态失效后重新点击“打开扫码窗口”并扫码，不绕过微信安全验证。
